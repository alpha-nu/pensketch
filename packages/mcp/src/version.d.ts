// Substituted at build time from the manifest by tsup, and by vitest for the
// suite. Declared rather than imported so that one string reaches the bundle
// instead of the whole package.json.
declare const __MCP_VERSION__: string;
